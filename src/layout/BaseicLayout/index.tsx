/* 此布局适用于有左侧菜单的布局 左右布局*/

import React from "react";
import styles from "./index.scss";

const BasicLayout = () => {
  return <div className={styles.BasicLayout}>左右布局</div>;
};

export default BasicLayout;
